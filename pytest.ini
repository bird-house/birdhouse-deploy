[pytest]
markers =
    online: marks test as requiring access to the internet (to skip these tests: '-m "not online"')
    minimal: marks test as only requiring the minimal birdhouse stack to run (no 'BIRDHOUSE_EXTRA_CONF_DIRS')
