services:
  magpie:
    volumes:
    - ./services/raven/config/magpie/providers.cfg:${MAGPIE_PROVIDERS_CONFIG_PATH}/raven-providers.cfg:ro
    - ./services/raven/config/magpie/permissions.cfg:${MAGPIE_PERMISSIONS_CONFIG_PATH}/raven-permissions.cfg:ro
