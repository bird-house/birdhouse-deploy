permissions:
  - service: finch
    permission: getcapabilities
    group: ${MAGPIE_DEFAULT_PERMISSION_GROUP}
    action: create

  - service: finch
    permission: describeprocess
    group: ${MAGPIE_DEFAULT_PERMISSION_GROUP}
    action: create

  - service: finch
    permission: execute
    group: ${MAGPIE_DEFAULT_PERMISSION_GROUP}
    action: create
