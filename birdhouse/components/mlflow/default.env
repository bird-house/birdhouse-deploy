# All env in this default.env can be overridden by env.local.

# All env in this default.env must NOT depend on any other env.  If they do, they
# must use single quotes to avoid early expansion before overrides in env.local
# are applied and must be added to the list of DELAYED_EVAL.

# Folder on the host to persist mlflow user data 
export MLFLOW_USER_DATA_DIR='${DATA_PERSIST_ROOT}/mlflow_user_data'
#export MLFLOW_USER_DATA_DIR='/data/mlflow_user_data'
