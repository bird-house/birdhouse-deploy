providers:
  # definition of STAC service for API access
  stac:
    url: http://stac:8000
    title: STAC
    public: true
    c4i: false
    type: api
    sync_type: api

permissions:
  # Administrator permissions over whole service
  - service: stac
    permission: read
    group: anonymous
    action: create
  - service: stac
    permission: write
    group: anonymous
    action: create
