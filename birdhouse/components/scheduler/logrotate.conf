/var/log/birdhouse/*.log {
    rotate ${BIRDHOUSE_SCHEDULER_LOGROTATE_ROTATE_DAILY_COUNT}
    daily
    missingok
    minsize ${BIRDHOUSE_SCHEDULER_LOGROTATE_ROTATE_MINSIZE}
    copytruncate
    compress
    dateext
}
