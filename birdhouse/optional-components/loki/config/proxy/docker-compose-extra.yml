version: "3.4"
services:
  proxy:
    volumes:
      - proxy-logs:${PROXY_LOG_DIR}
  promtail:
    volumes:
      - proxy-logs:${LOKI_PROMTAIL_LOG_DIR}/proxy
    environment:
      - PROXY_LOG_FILE=${PROXY_LOG_FILE}

volumes:
  proxy-logs:
