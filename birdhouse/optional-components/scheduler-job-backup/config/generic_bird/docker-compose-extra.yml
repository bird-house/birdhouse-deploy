services:
  scheduler:
    volumes: 
      - ./optional-components/scheduler-job-backup/config/generic_bird/${_SCHEDULER_JOB_BACKUP_STATEFUL_DATA_FILE}:/scheduler-job-configs/backup-generic_bird.yml:ro
