server:
  http_listen_port: ${PROMTAIL_PORT}
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
${PROMTAIL_LOKI_CLIENT_URL_BLOCK}

scrape_configs: [{
  job_name: system,
  static_configs: [
    targets: [localhost],
    labels: {
      job: varlogs,
      __path__: '${PROMTAIL_LOG_DIR}/**/*.log'
    }
  ],
  pipeline_stages: [${PROMTAIL_VARLOGS_PIPELINE_STAGES}]
  }]
