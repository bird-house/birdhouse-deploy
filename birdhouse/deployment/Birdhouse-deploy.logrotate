/var/log/Birdhouse/*.log {
    # keep at least 6 weeks' worth of logs (do not rotate
    # until the file is > 500k)
    rotate 6
    weekly
    missingok
    minsize 500k
    copytruncate
    compress
    dateext
}
